const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#0f172a',
    padding: 20,
  },

  header: {
    marginBottom: 30,
  },

  title: {
    fontSize: 26,
    fontWeight: 'bold',
    color: '#fff',
  },

  subtitle: {
    color: '#cbd5e1',
    marginTop: 5,
  },

  cards: {
    gap: 15,
  },

  card: {
    backgroundColor: '#1e293b',
    padding: 20,
    borderRadius: 14,
  },

  cardTitle: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
  },

  cardDesc: {
    color: '#94a3b8',
    marginTop: 5,
  },

  logout: {
    marginTop: 'auto',
    backgroundColor: '#dc2626',
    padding: 15,
    borderRadius: 10,
  },

  logoutText: {
    color: '#fff',
    textAlign: 'center',
    fontWeight: 'bold',
  },
});
